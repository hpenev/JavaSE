package arraysHomework;

import java.util.Arrays;

public class Task15 {
	public static void main(String[] args) {
		// Scanner sc = new Scanner(System.in);
		// System.out.print("Enter array length: ");
		// int arrayLength = sc.nextInt();
		//
		// double[] array = new double[arrayLength];
		// for (int i = 0; i < arrayLength; i++) {
		// System.out.print("Enter a number: ");
		// array[i] = sc.nextInt();
		// }
		// sc.close();

		double[] array = { 7.13, 0.2, 4.9, 5.1, 6.34, 1.12 };

		double[] maxNumbers = new double[3];

		for (int i = 0; i < array.length; i++) {

			double temp = 0;
			
			// implement bubbleSort
			for (int j = 0; j < maxNumbers.length; j++) {
				for (int k = 1; k < maxNumbers.length - 1; k++) {
					if (maxNumbers[j - 1] < maxNumbers[j]) {
						temp = maxNumbers[j - 1];
						maxNumbers[j - 1] = maxNumbers[j];
						maxNumbers[j] = temp;
					}
				}
			}

			// if (array[i] > maxNumbers[pos]) {
			// maxNumbers[pos] = array[i];
			// }
		}

		System.out.println(Arrays.toString(maxNumbers));

	}
}
